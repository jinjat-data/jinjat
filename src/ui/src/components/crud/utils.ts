export interface JinjatFormProps {
    packageName: string;
    analysis: string;
}

export interface JinjatExposureProps {
    packageName: string;
    exposure: string
}

export interface JinjatListProps {
    packageName: string;
    analysis: string;
    enableActions:boolean
}